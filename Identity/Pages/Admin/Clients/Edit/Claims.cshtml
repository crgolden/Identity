@page
@model ClaimsModel

@{
    ViewData["Title"] = "Edit Client Claims";
}

<h1>Edit Client Claims</h1>

<form method="post">
    <table class="table table-bordered table-sm" id="claims-table">
        <thead class="table-light">
            <tr>
                <th>Type</th>
                <th>Value</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @for (var i = 0; i < Model.Claims.Count; i++)
            {
                <tr>
                    <td>
                        <input type="hidden" name="Claims[@i].Id" value="@Model.Claims[i].Id" />
                        <input name="Claims[@i].Type" value="@Model.Claims[i].Type" class="form-control form-control-sm" />
                    </td>
                    <td>
                        <input name="Claims[@i].Value" value="@Model.Claims[i].Value" class="form-control form-control-sm" />
                    </td>
                    <td>
                        <button type="button" class="btn btn-sm btn-danger remove-row">Remove</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <button id="btn-add-row" type="button" class="btn btn-sm btn-outline-secondary mb-3">Add</button>

    <template id="row-template">
        <tr>
            <td>
                <input type="hidden" name="Claims[__index__].Id" value="0" />
                <input name="Claims[__index__].Type" class="form-control form-control-sm" />
            </td>
            <td>
                <input name="Claims[__index__].Value" class="form-control form-control-sm" />
            </td>
            <td>
                <button type="button" class="btn btn-sm btn-danger remove-row">Remove</button>
            </td>
        </tr>
    </template>

    <div>
        <button id="save-submit" type="submit" asp-route-id="@Model.Client.Id" class="btn btn-primary">Save</button>
        <a asp-page="/Admin/Clients/Details/Claims" asp-route-id="@Model.Client.Id" class="btn btn-outline-secondary ms-1">Cancel</a>
    </div>
</form>

@section Scripts {
    <script src="~/js/admin-collection.js"></script>
}
