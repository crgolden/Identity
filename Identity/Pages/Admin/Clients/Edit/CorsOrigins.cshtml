@page
@model CorsOriginsModel

@{
    ViewData["Title"] = "Edit Client CORS Origins";
}

<h1>Edit Client CORS Origins</h1>

<form method="post">
    <table class="table table-bordered table-sm" id="cors-table">
        <thead class="table-light">
            <tr>
                <th>Origin</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @for (var i = 0; i < Model.CorsOrigins.Count; i++)
            {
                <tr>
                    <td>
                        <input type="hidden" name="CorsOrigins[@i].Id" value="@Model.CorsOrigins[i].Id" />
                        <input name="CorsOrigins[@i].Origin" value="@Model.CorsOrigins[i].Origin" class="form-control form-control-sm" />
                    </td>
                    <td>
                        <button type="button" class="btn btn-sm btn-danger remove-row">Remove</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <button id="btn-add-row" type="button" class="btn btn-sm btn-outline-secondary mb-3">Add</button>

    <template id="row-template">
        <tr>
            <td>
                <input type="hidden" name="CorsOrigins[__index__].Id" value="0" />
                <input name="CorsOrigins[__index__].Origin" class="form-control form-control-sm" />
            </td>
            <td>
                <button type="button" class="btn btn-sm btn-danger remove-row">Remove</button>
            </td>
        </tr>
    </template>

    <div>
        <button id="save-submit" type="submit" asp-route-id="@Model.Client.Id" class="btn btn-primary">Save</button>
        <a asp-page="/Admin/Clients/Details/CorsOrigins" asp-route-id="@Model.Client.Id" class="btn btn-outline-secondary ms-1">Cancel</a>
    </div>
</form>

@section Scripts {
    <script src="~/js/admin-collection.js"></script>
}
