@page
@model PostLogoutRedirectUrisModel

@{
    ViewData["Title"] = "Edit Client Post-Logout Redirect URIs";
}

<h1>Edit Client Post-Logout Redirect URIs</h1>

<form method="post">
    <table class="table table-bordered table-sm" id="plru-table">
        <thead class="table-light">
            <tr>
                <th>Post-Logout Redirect URI</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @for (var i = 0; i < Model.PostLogoutRedirectUris.Count; i++)
            {
                <tr>
                    <td>
                        <input type="hidden" name="PostLogoutRedirectUris[@i].Id" value="@Model.PostLogoutRedirectUris[i].Id" />
                        <input name="PostLogoutRedirectUris[@i].PostLogoutRedirectUri" value="@Model.PostLogoutRedirectUris[i].PostLogoutRedirectUri" class="form-control form-control-sm" />
                    </td>
                    <td>
                        <button type="button" class="btn btn-sm btn-danger remove-row">Remove</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <button id="btn-add-row" type="button" class="btn btn-sm btn-outline-secondary mb-3">Add</button>

    <template id="row-template">
        <tr>
            <td>
                <input type="hidden" name="PostLogoutRedirectUris[__index__].Id" value="0" />
                <input name="PostLogoutRedirectUris[__index__].PostLogoutRedirectUri" class="form-control form-control-sm" />
            </td>
            <td>
                <button type="button" class="btn btn-sm btn-danger remove-row">Remove</button>
            </td>
        </tr>
    </template>

    <div>
        <button id="save-submit" type="submit" asp-route-id="@Model.Client.Id" class="btn btn-primary">Save</button>
        <a asp-page="/Admin/Clients/Details/PostLogoutRedirectUris" asp-route-id="@Model.Client.Id" class="btn btn-outline-secondary ms-1">Cancel</a>
    </div>
</form>

@section Scripts {
    <script src="~/js/admin-collection.js"></script>
}
