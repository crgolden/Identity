@page "{id:int}"
@model Identity.Pages.Admin.IdentityResources.Details.ClaimTypesModel
@{
    ViewData["Title"] = "Identity Resource Claim Types";
}

<h1>Claim Types: @Model.Resource.Name</h1>

<table class="table table-bordered table-hover table-sm">
    <thead class="table-light">
        <tr><th>Type</th></tr>
    </thead>
    <tbody>
        @foreach (var claim in Model.Resource.UserClaims ?? [])
        {
            <tr><td>@claim.Type</td></tr>
        }
    </tbody>
</table>

<a id="btn-back" asp-page="./Index" asp-route-id="@Model.Resource.Id" class="btn btn-outline-secondary btn-sm">Back</a>
<a id="btn-edit" asp-page="../Edit/ClaimTypes" asp-route-id="@Model.Resource.Id" class="btn btn-outline-secondary btn-sm">Edit</a>
